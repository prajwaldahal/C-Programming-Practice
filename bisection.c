#include<stdio.h>
int main()
{
	int a,b;char str[20];double m,roottemp;
	printf("enter as a and b: ");
	scanf("%d%d",&a,&b);
	//printf("enter a eqn:");
	//fflush(stdin);
	//gets(str);
	//a=atoi(str);
	while(1)
	{
			
	}	
}
